namespace AfflictionComponent.Interfaces;

// Probably don't need these actually, we can check if an affliction has a risk by seeing if this interface is implmented.
public interface IRisk
{
    /*protected bool Risk { get; set; }

    protected sealed bool HasRisk() => Risk;*/
}